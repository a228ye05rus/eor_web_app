@using eor_web_app.ViewModels
@model IndexViewModel

<div class="container">
    @foreach (Profile p in Model.Profiles)
    {
        <ul>
            <li>
                <p class="menu-item" data-id="@p.Id">@p.Name</p>
            </li>
            <ul>
                @foreach (Group g in Model.Groups.Where(g => g.ProfileId == p.Id))
                {
                    <li>
                        <p class="page" data-menu="@g.ProfileId">
                            @g.Name
                        </p>
                    </li>
                }
            </ul>
        </ul>
    }
</div>



@*<ul class="me-auto mb-2 mb-lg-0 ms-lg-4">
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Shop</a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="#!">All Products</a></li>
            <li><hr class="dropdown-divider" /></li>
            <li><a class="dropdown-item" href="#!">Popular Items</a></li>
            <li><a class="dropdown-item" href="#!">New Arrivals</a></li>
        </ul>
    </li>
</ul>*@